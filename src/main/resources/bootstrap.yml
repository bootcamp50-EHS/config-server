server:
  port: 8888
spring:
  application:
    name: config-server
    security:
      user:
        name: ehs
        password: ehs123
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bootcamp50-EHS/config-repository.git
          default-label: "develop"
          